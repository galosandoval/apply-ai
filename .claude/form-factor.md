Desktop-first. apply-ai is a sit-down, keyboard-and-pointer task.
Unprefixed Tailwind = the desktop layout; `max-lg:`/`max-md:`/`max-sm:` adapt down.
Hover, focus rings, and dense controls are first-class. Keep `dvh` over `vh`.
Verify at 1440×900 first, then spot-check ~375px.
